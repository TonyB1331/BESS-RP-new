# Copy this file to .streamlit/secrets.toml and fill in your values.
# On Streamlit Community Cloud: App > Settings > Secrets (paste the same keys).
# NEVER commit the real secrets.toml (it is in .gitignore).

MOTHERDUCK_TOKEN = "paste-your-motherduck-service-token-here"
ENTSOE_API_TOKEN = "paste-your-entsoe-token-here"
